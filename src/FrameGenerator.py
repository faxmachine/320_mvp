import PIL
from PIL import Image

class FrameGenerator:

	def GenerateFrame(features):
		# Float from 0 to 10,000
		pitch = features[0]
		energy = []




im = Image.new("RGB", (1920, 1080), (0,0,255))
im.show() 

		














